spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rec_system?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 141414
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis:
  configuration:
    map-underscore-to-camel-case: true